// src/components/NumberOfEvents.js

const NumberOfEvents = () => {
  return (
    <input
      type="text"
      id="number-of-events"
      defaultValue={32}
      onChange={() => {}}
    />
  );
};

export default NumberOfEvents;
